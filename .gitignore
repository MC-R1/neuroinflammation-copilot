# OS / editor junk
.DS_Store
**/.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo

# Local Claude Code machine state (the shareable agents/ and launch.json ARE kept)
.claude/settings.local.json

# Office temp / lock files
~$*

# Source / reference materials (event docs + third-party decks + metrics source sheets) — inputs,
# NOT needed to run the prototype. Excluded by default to avoid republishing third-party material.
/*.pdf
/*.docx
/digital mobility/
